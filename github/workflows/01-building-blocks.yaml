---
name: 01 – Building Blocks
on: 
  push:
  workflow_dispatch:

jobs:
  powitanie:
  runs-on: ubuntu-latest
  steps:
  - name: Say hello
    run: echo "Hello, World!"
  
  pozegnanie:
  runs-on: ubuntu-latest
  steps:
  - name: Failed Step
        run: |
          echo "I will fail now"
          exit 1
      - name: Say Goodbye
        run: "This step will be skipped if the previous step fails"